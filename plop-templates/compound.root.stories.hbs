import type { Meta, StoryObj } from "@storybook/react"
import { {{pascalCase name}}, type {{pascalCase name}}Props } from "./index"

const meta: Meta<typeof {{pascalCase name}}> = {
  title: "{{pascalCase layer}}/{{pascalCase name}}",
  component: {{pascalCase name}},
}

export default meta
type Story = StoryObj<typeof meta>

const defaultArgs: {{pascalCase name}}Props = {
  children: "Children of {{pascalCase name}}",
}

export const Default: Story = {
  args: defaultArgs,
}

export const ErrorStory: Story = {
  args: {
    ...Default.args,
  },
}
